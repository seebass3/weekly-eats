# Secrets (used by docker-compose.yml)
POSTGRES_PASSWORD=
APP_PASSWORD=
SESSION_SECRET=
CRON_SECRET=

# Optional overrides (defaults shown)
OLLAMA_MODEL=gemma3:4b

# For local development only (not used by Docker — compose constructs these)
DATABASE_URL=postgresql://weekly_eats:YOUR_PASSWORD@localhost:54320/weekly_eats
OLLAMA_BASE_URL=http://localhost:11434
